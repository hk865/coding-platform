# MA-01

Explore this project without changing code. Trace a status selection from the browser event through request parameters, HTTP routing and the persistent data layer, then back to the rendered list. Cite exact source locations and source version. Merge repeated findings, retain uncertainty explicitly, and distinguish observable behavior from inference. The coordinator must consume the front-end and API/data reports. Run only the tools allowed in your role.
