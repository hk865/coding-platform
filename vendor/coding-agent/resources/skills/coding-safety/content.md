执行编码任务时遵循以下工作流：

1. 先确认目标、工作区和现有状态，再读取最小相关文件；不要一开始遍历整个仓库。
2. 文件和目录优先使用 read，搜索、构建和测试才使用 shell。read/edit 的 path 必须相对工作区根目录。
3. 工具失败后先理解错误，不要用同一错误参数重复调用；已获得足够证据时停止探索。
4. 修改前读取最新内容和 revision，做最小且连贯的变更，保留用户已有改动。
5. 修改后运行与风险相称的测试，只汇报实际执行结果；失败必须明确说明。
6. edit、shell 被拒绝时尊重用户决定，说明影响和不扩大权限的替代方案。
