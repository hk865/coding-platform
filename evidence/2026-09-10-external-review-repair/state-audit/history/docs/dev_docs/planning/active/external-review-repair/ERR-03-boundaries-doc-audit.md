# ERR-03：能力路由、接口边界与文档精度核对

状态：active；角色：只读调查。第一阶段不实现、不修改产品源码或权威文档。

## 调查范围

核对 B-1、A-1、A-2、A-3/R-6、A-4/B-2、R-1、R-5、ReadModel 已证实重复纯投影、D-1…D-8。区分设计允许依赖、规划依赖和当前实现依赖；不得为了边数制造调用或删除有效产品要求。

## 输出与验收

1. 为显式执行能力/来源事实提出最小契约：非特殊前缀真实 plan 不进 fixture；fixture 必须显式开启；UI 和实际能力一致。
2. 列出 A-1 正式 Control 接口的最小形状及所有宿主/测试消费者，保证宿主不向业务写函数传原始可写 Ledger。
3. 核实 CodeGraphPort 的全部真实消费者；给出删除 dead seam 或 unconfigured/unsupported 统一降级的选择依据。
4. 给 JS、Python、Storage 共享工具列出 owner、实际检查覆盖/排除范围；不得把文件归属检查称为完整沙箱验证。
5. 精确列出 R-1/R-5/ReadModel 可安全共享的纯规则与双后端行为对照测试，不建议全仓命名翻新或全面性能重构；轮询/全量来源读取只登记测量任务。
6. 对 D-1…D-8 逐项给出目标文件、准确措辞与历史保留方式，并列出本批延期项的理由、风险和后续验收条件。
7. 向主 Agent 返回只读报告和建议写入清单；后续如被分配实现，需新写入范围且不能审查自己的实现。
